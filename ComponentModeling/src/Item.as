package
{
	import starling.display.Quad;
	
	public class Item extends Quad
	{
		public function Item(width:Number, height:Number, color:uint=16777215, premultipliedAlpha:Boolean=true)
		{
			super(width, height, color, premultipliedAlpha);
		}
	}
}